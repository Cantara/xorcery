adminservlet:
  enabled: "{{ defaults.enabled }}"
serveradmin:
  enabled: "{{ defaults.enabled }}"

jersey.server.register:
  - com.exoreaction.xorcery.admin.api.VisualeHealthResource

opentelemetry.instrumentations.jersey.excludes:
  - /health
