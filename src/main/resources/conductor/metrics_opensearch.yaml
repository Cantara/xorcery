data:
  - id: metrics_opensearch
    type: grouptemplate,
    attributes:
      name: Log metrics to OpenSearch
      sources:
        pattern: rel=='metricevents'
        many: true
      consumers:
        pattern: type=='opensearch'
