data:
  - id: log4j_opensearch
    type: grouptemplate
    attributes:
      name: Log4j to OpenSearch
      sources:
        pattern: type=='log4jappender'
        configuration:
          rel: opensearch
      consumers:
        pattern: type=='opensearch'
        configuration:
          index: "logs-{{environment}}-{{tag}}-%1$tF"
