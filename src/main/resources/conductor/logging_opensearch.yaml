data:
  - id: logging_opensearch
    type: grouptemplate
    attributes:
      name: Log messages to OpenSearch
      sources:
        pattern: rel=='logevents'
        many: true
      consumers:
        pattern: type=='opensearch'
