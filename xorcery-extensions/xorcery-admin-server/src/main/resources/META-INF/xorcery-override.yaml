$schema: ./xorcery-override-schema.json

jersey:
  server:
    register:
    - com.exoreaction.xorcery.admin.api.VisualeHealthResource

opentelemetry:
  global:

  sampler:
    excludes:
    - name: "GET /health"
