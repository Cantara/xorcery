$schema: xorcery-admin-server-override-schema.json

jersey:
  server:
    register:
    - dev.xorcery.admin.api.VisualeHealthResource

opentelemetry:
  sampler:
    excludes:
    - name: "GET /health"
