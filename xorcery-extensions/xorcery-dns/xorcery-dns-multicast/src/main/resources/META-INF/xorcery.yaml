$schema: xorcery-dns-multicast-schema.json
dns:
  multicast:
    enabled: "{{ defaults.enabled }}"
    discovery:
      enabled: "{{ dns.multicast.enabled }}"
    announce:
      enabled: "{{ dns.multicast.enabled }}"
