$schema: xorcery-dns-update-schema.json
dns:
  dyndns:
    enabled: "{{ dns.client.enabled }}"
    timeout: 30s
    ttl: 60s
# TSIG key for DDNS updates
#    key:
#      name: "xorcery.test"
#      secret: "name of secret goes here"
#      algorithm: "HMAC-MD5.SIG-ALG.REG.INT."
