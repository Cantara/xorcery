$schema: xorcery-neo4j-projections-schema.json
neo4jprojections:
  enabled: "{{ neo4jdatabase.enabled }}"
  eventBatchSize: 1024
  # Use Reactor "bounded elastic" by default, set this to specific nr if there are too many projections at the same time
  maxThreadCount: -1
