neo4jprojections:
  enabled: "{{ defaults.enabled }}"
  disruptor: {}
  eventBatchSize: 1024
  commitpublisher:
    enabled: "{{ neo4jprojections.enabled }}"
  eventsubscriber:
    enabled: "{{ neo4jprojections.enabled }}"
  subscribers: []
#  - uri: "wss://somehost:someport"
#    stream: somestream
#    projection: someprojection
#    configuration: {}
