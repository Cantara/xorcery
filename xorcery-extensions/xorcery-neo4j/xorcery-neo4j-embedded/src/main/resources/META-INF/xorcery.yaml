neo4jdatabase:
  enabled: "{{ defaults.enabled }}"
  path: "{{ instance.home }}/neo4j"
  startup: []
  domain:
    version: "1.0.0"
    wipeOnBreakingChange: true
  settings: {}
  metrics:
    enabled: "{{ defaults.enabled }}"
