{
  "$schema" : "https://json-schema.org/draft/2020-12/schema",
  "type" : "object",
  "$id" : "http://xorcery.exoreaction.com/modules/com.exoreaction.xorcery/xorcery-domainevents-publisher/schema",
  "title" : "xorcery-domainevents-publisher configuration JSON Schema",
  "properties" : {
    "domainevents" : {
      "$ref" : "#/$defs/domainevents"
    }
  },
  "$defs" : {
    "domainevents" : {
      "type" : "object",
      "properties" : {
        "enabled" : {
          "type" : "string",
          "default" : "{{ defaults.enabled }}",
          "description" : "Default: {{ defaults.enabled }} ({{ defaults.enabled }})"
        },
        "eventstore" : {
          "type" : "string",
          "default" : "{{ instance.api }}eventstore",
          "description" : "Default: {{ instance.api }}eventstore ({{ instance.api }}eventstore)"
        },
        "projections" : {
          "type" : "string",
          "default" : "{{ instance.api }}projections",
          "description" : "Default: {{ instance.api }}projections ({{ instance.api }}projections)"
        }
      }
    }
  }
}