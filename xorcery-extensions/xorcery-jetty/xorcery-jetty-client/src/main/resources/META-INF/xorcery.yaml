$schema: xorcery-schema.json
jetty:
  clients:
  - name: "default"
    enabled: "{{ defaults.enabled }}"
    idleTimeout: "{{ defaults.idleTimeout }}"
    connectTimeout: "5s"
    requestBufferSize: 4096
    reusePort: false
    http2:
      enabled: false

    ssl:
      enabled: "{{ jetty.clients.default.ssl.keystore ? true | false }}"
      # Client
      alias: "self"
      keystore: null
      truststore: null
      #Server
      endpointIdentificationAlgorithm: "HTTPS"
      trustAll: false
