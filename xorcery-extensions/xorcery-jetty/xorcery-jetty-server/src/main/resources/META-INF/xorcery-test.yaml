$schema: ./xorcery-schema.json
jetty:
  server:
    http:
      port: "{{ CALCULATED.dynamicPorts.http }}"
    ssl:
      port: "{{ CALCULATED.dynamicPorts.ssl }}"
    minThreads: 3
    security:
      jwt:
        issuers:
