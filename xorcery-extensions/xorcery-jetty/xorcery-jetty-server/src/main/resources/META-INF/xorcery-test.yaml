$schema: xorcery-jetty-server-override-schema.json
jetty:
  server:
    http:
      port: "{{ CALCULATED.dynamicPorts.http }}"
    ssl:
      port: "{{ CALCULATED.dynamicPorts.ssl }}"
    minThreads: 3
