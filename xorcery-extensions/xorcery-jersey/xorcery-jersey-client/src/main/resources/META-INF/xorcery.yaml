$schema: xorcery-schema.json
jersey:
  clients:
  - name: "default"
    enabled: "{{ defaults.enabled }}"
    keystore: null
    truststore: null
    connectTimeout: "{{ defaults.connectTimeout }}"
    readTimeout: "30s"
    httpClient: "default"
    properties: {}
