$schema: xorcery-opentelemetry-system-schema.json
opentelemetry:
  instrumentations:
    system:
      enabled: "{{ defaults.enabled }}"
      attributes:
        system.filesystem.usage: system.filesystem.usage
