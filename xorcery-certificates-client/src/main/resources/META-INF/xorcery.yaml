certificates:
  client:
    enabled: "{{ defaults.enabled }}"
    host: "srv://_certificates"
    renewonstartup: false
keystores:
  keystore:
    path: "META-INF/keystore.p12"
    password: "password"
  truststore:
    path: "META-INF/truststore.p12"
    password: "password"
    addrootca: true
