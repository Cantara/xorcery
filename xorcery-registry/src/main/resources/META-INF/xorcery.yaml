registry:
    enabled: "{{ defaults.enabled }}"
    upstream:
        enabled: false
        url: null
    publisher:
        enabled: "{{ registry.enabled }}"
    dnsdiscovery:
        enabled: false
