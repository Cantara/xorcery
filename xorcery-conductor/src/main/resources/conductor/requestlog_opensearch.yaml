data:
  - id: requestlog_opensearch
    type: grouptemplate
    attributes:
      name: Log requests to OpenSearch
      sources:
        pattern: type=='requestlog'
        configuration:
          rel: opensearch
      consumers:
        pattern: type=='opensearch'
        configuration:
          index: "requestlogs-{{environment}}-{{tag}}-%tF"
