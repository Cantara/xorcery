dns:
  enabled: "{{ defaults.enabled }}"
  server:
    enabled: "{{ dns.enabled }}"
    port: 8853
    zones:
      - name: xorcery.test
        allow-update:
          - key: updatekey
    keys:
      - name: updatekey
        secret: "secrets:dnsupdatekey"
        algorithm: "hmac-md5"
