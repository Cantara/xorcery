dns:
    enabled: "{{ defaults.enabled }}"
    nameservers: []
    hosts: {}
    server:
       enabled: "{{ defaults.enabled }}"
       port: 53
       discovery:
           enabled: "{{ defaults.enabled }}"
       multicast:
           enabled: "{{ defaults.enabled }}"
