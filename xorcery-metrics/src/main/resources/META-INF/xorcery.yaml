metrics:
  enabled: "{{ defaults.enabled }}"
  filter: []
  subscriber:
    authority: null
    stream: "metrics"
    configuration:
      instance:
        id: "{{ instance.id }}"
  publisher:
    configuration: {}
