metrics:
  enabled: "{{ defaults.enabled }}"
  filter: []
  subscriber:
    uri: null
    stream: "metrics"
    configuration:
      instance:
        id: "{{ instance.id }}"
  publisher:
    configuration: {}
