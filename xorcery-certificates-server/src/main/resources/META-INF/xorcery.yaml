certificates:
  server:
    enabled: "{{ defaults.enabled }}"
    authorization: none

jersey.server.register:
  - com.exoreaction.xorcery.service.certificates.server.resources.CertificateRequestResource
