dns:
  enabled: "{{ defaults.enabled }}"
  registration:
    enabled: "{{ dns.enabled }}"
    domain: "{{ ENV.DOMAIN | \"xorcery.test\" }}"
# TSIG key for DDNS updates
#    key:
#      name: "xorcery.test"
#      secret: "keygoeshere"
#      algorithm: "HMAC-MD5.SIG-ALG.REG.INT."
    announce:
      enabled: "{{ dns.registration.enabled }}"
