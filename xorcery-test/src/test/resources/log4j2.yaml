Configuration:
  name: Xorcery tests
  status: warn
  properties:
  thresholdFilter:
    level: debug
  appenders:
    Console:
      name: STDOUT
      target: SYSTEM_OUT
      PatternLayout:
        Pattern: "%d [%t] %-5level %marker %c{1.}: %msg%n%throwable"

  Loggers:
    logger:
      - name: org.apache.logging.log4j
        level: debug
        additivity: false
        AppenderRef:
          ref: STDOUT

      - name: com.exoreaction.xorcery.core
        level: debug

      - name: com.exoreaction.xorcery.dns.server
        level: info

      - name: client
        level: debug

      - name: client.opensearch
        level: debug

      - name: com.exoreaction.xorcery.conductor.GroupTemplates
        level: debug

      - name: com.exoreaction.xorcery.reactivestreams.resources.websocket
        level: info

      - name: javax
        level: debug

      - name: org.eclipse.jetty.security.authentication
        level: debug

      - name: org.eclipse.jetty.util.security
        level: debug

    Root:
      level: info
      AppenderRef:
        - ref: STDOUT
