neo4jdatabase:
  enabled: true
  path: "{{ home }}/neo4j"
  startup: []
  settings:
    dbms:
      allow_upgrade: true

neo4jprojections:
  enabled: true
  publisher.enabled: "{{ neo4jprojections.enabled }}"
  subscriber.enabled: "{{ neo4jprojections.enabled }}"
