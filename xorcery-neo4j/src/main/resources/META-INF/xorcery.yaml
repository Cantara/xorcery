neo4jdatabase:
  enabled: "{{ defaults.enabled }}"
  path: "{{ home }}/neo4j"
  startup: []
  temporary_settings_file: "tmp_neo4j.conf"
  settings:
    dbms:
      allow_upgrade: true

jersey.server.register:
  - com.exoreaction.xorcery.service.neo4j.resources.api.Neo4jResource
